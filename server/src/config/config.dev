let config = {};

config.dbUri = process.env.dbUri || 'mongodb://localhost:27017/cars';
config.serverPort = process.env.serverPort || 4000;

config.totalCars = 20;
config.preparingTime = 3600000;

export default config;